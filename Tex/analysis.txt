THESIS REVISIONS NEEDED
========================================

Chapter 2.2.1 - Koblitz Curve Optimization
Add after secp256k1 curve equation: Note that secp256k1 is categorized as a Koblitz curve with specific structure that enables optimizations like the GLV method (not implemented in this project). Standard double-and-add used for clarity.

Chapter 2.3.1 - Expand Shor's Algorithm
Currently only 2 paragraphs. Need to add: Period Finding Problem explanation, Quantum Fourier Transform mechanics, step-by-step breakdown (Superposition → Oracle → QFT → Measurement), complexity comparison O((log N)³) quantum vs O(exp) classical, resource requirements (~2n qubits for n-bit number).

Chapter 2.3.2 - Expand QKD Discussion
Currently 1 paragraph dismissing QKD. Add: BB84 protocol mechanism (2-3 sentences), three limitations (Authentication Paradox requiring PQC, Trusted Nodes with 0.2 dB/km attenuation, Infrastructure Cost with single-photon detectors), appropriate use cases (government/banking point-to-point), balanced technical critique tone.

Chapter 2.4.4 - Explain Why q=3329
Add after mentioning q=3329 modulus: Explain q=3329 is chosen because q ≡ 1 (mod 2n), since 3329 = 13×256 + 1 this enables Number Theoretic Transform, without this property polynomial multiplication would be O(n²).

Chapter 3.1.1 - Clarify lib.c/lib.py Roles
Emphasize relationship: lib.c acts as the performance engine, lib.py acts as the interface and driver, lib.py contains built-in test suite for verification, together they form a unified cryptographic library.

Chapter 3.3.1 - Add Montgomery Reduction
Add paragraph after NTT explanation: Implementation uses Montgomery Reduction technique, allows modular multiplication without expensive division instructions, critical for maintaining performance in modulo q operations.

Chapter 3.4 - Restructure SIMD Section
Reduce: Cut compiler flags (/arch:AVX2, /O2) and compilation command verbatim. Keep: Explanation of why AVX2 helps (16 coefficients parallel). Add: Multithreading subsection explaining Kyber is embarrassingly parallel, matrix operations independent, server can handle multiple handshakes in parallel threads, no shared state between key exchanges.

Chapter 3.6 - Make Code Structure More Generic
Replace specific file listing with: Modular architecture with separation of concerns, core cryptographic library (lib.c + lib.py), application layer (server, client, dashboard), testing infrastructure integrated in lib.py.

Chapter 4.2.1 - Add Non-SIMD Baseline Data
Table currently only shows SIMD-optimized results. Add rows for: ECC (Python), Kyber (Python), Kyber (C no SIMD). Show 3-way comparison demonstrating Python → Standard C → AVX2 C progression. Recalculate all percentages and speedup claims to show AVX2 is the key advantage, not just C vs Python.

Chapter 4.4.4 - Expand Bandwidth Discussion
Currently mentions 23x overhead. Add specifics: 1.5KB exceeds standard Ethernet MTU (1500 bytes) requiring fragmentation, IoT protocols (LoRa, ZigBee) with 250-byte max are unusable, high-bandwidth networks (fiber, 5G) have negligible impact.

Chapter 4.5.2 - Add Hybrid Strategy Table
Create cost/benefit comparison table with columns: ECC Only, Kyber Only, Hybrid. Rows: Bandwidth (66B, 1568B, ~1634B), Latency (460ms, 242ms, ~460ms), Security (Quantum Broken, Quantum Resistant, Defense in Depth).

Chapter 5.3 - Rewrite Challenges Section
Replace generic future work with specific technical challenges encountered: Theoretical (distinguishing LWE vs Ring-LWE vs Module-LWE in literature), Implementation (Kyber parameters q,n,η tightly coupled, adjusting one broke decryption/NTT alignment, lack of standard parameter sets beyond 512/768/1024), Operational (server state management with concurrent handshakes, debugging decryption failures in C).

Bibliography - Add Missing References
Need to add: Fujisaki-Okamoto transform, Cooley-Tukey FFT, Montgomery Reduction paper, Intel AVX2 specification, Bitcoin/secp256k1 context papers, FIPS 203 (2024 final standard), MTU fragmentation RFC, side-channel attack surveys, TLS 1.3 PQC integration, QKD cost analysis sources. Note: Ajtai1996 already in bibliography but never cited - add citation where appropriate.

All Chapters - Terminology Consistency
Use "CRQC" (Cryptographically Relevant Quantum Computer) consistently throughout. Use "HNDL" (Harvest Now, Decrypt Later) as acronym. First use: full term + (acronym), subsequent: acronym only.

All Chapters - Citation Placement
Cite immediately after claim, not at paragraph end. Check: Chapter 2.4.3 cite Langlois2015 right after "Module-LWE", Chapter 3.3.2 Fujisaki-Okamoto right after "CCA security", anywhere "NIST selected" appears cite NIST-Selection immediately.

All Chapters - Quantify Vague Phrases
Replace: "significantly smaller" → with actual numbers (e.g., "10x smaller"), "highly parallelizable" → "operations share no state", "modern networks" → "networks with >10 Mbps bandwidth", "computationally intractable" → "requires exponential time O(2^n)".